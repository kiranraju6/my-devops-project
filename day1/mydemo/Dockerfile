# Use the official Nginx image to serve the HTML file
FROM nginx:alpine

# Copy the index.html file into the appropriate directory in the Nginx container
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 so the app is accessible
EXPOSE 80
